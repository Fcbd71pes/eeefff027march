# 🚀 বোট চালানোর নির্দেশনা

## দ্রুত শুরু

### 1️⃣ প্রথম সময়
```bash
# প্রয়োজনীয় প্যাকেজ ইনস্টল করুন
python -m pip install -r requirements.txt

# টেস্ট করুন (ঐচ্ছিক)
python test_bot.py

# বোট চালান
python bot.py
```

### 2️⃣ পরবর্তী সময়
```bash
# শুধু চালান
python bot.py
```

## ⚠️ সমস্যা সমাধান

### "Conflict: terminated by other getUpdates request"
**কারণ:** একই বোট একাধিকবার চলছে

**সমাধান:**
```bash
# পুরাতন bot বন্ধ করুন (CMD টার্মিনালে Ctrl+C চাপুন)
# তারপর নতুন শুরু করুন
python bot.py
```

### "ModuleNotFoundError: No module named 'telegram'"
**কারণ:** প্যাকেজ ইনস্টল নেই

**সমাধান:**
```bash
python -m pip install python-telegram-bot
```

### বোট লগ দেখছি না
**সমাধান:**
```bash
# Telegram এ /start কমান্ড দিন
# বা কোনো মেসেজ পাঠান
```

## 📋 উপলব্ধ কমান্ড

### খেলোয়াড়দের জন্য
```
/start           - রেজিস্ট্রেশন
/profile         - প্রোফাইল দেখুন
/leaderboard     - লিডারবোর্ড
/rules           - নিয়মাবলী
```

### অ্যাডমিনদের জন্য
```
/stats           - সিস্টেম পরিসংখ্যান
/userinfo <id>   - ব্যবহারকারী তথ্য
/broadcast <msg> - সকলকে মেসেজ পাঠান
/banuser <id>    - ব্যবহারকারী ব্যান
/unbanuser <id>  - আনব্যান করুন
```

## 🔧 কনফিগারেশন

`config.py` এ এই মান গুলি পরিবর্তন করুন:

```python
TOKEN = 'আপনার Telegram Bot Token'
ADMINS = [আপনার Telegram ID]
CHANNEL_ID = -100xxxxxxxxx
BOT_USERNAME = 'আপনার বটের ইউজারনেম'
```

## 📊 ডাটাবেস

- ফাইল: `local_data.db` (স্বয়ংক্রিয়ভাবে তৈরি হয়)
- ব্যাকআপ: `/backup` কমান্ড দিয়ে করুন

## 🎮 টেস্টিং

```bash
# টেস্ট রান করুন
python test_bot.py

# সফল হলে দেখবেন:
# ✅ সব টেস্ট পাস করেছে!
```

## 📱 Telegram এ টেস্ট করুন

1. বট খুঁজুন: `@esfootball_tournament_bot`
2. `/start` কমান্ড দিন
3. মেনু অনুসরণ করুন

## 🆘 সাহায্য লাগলে

- সব ফাইল আছে কিনা চেক করুন: bot.py, db.py, config.py
- config.py এ TOKEN ঠিক আছে কিনা চেক করুন
- লগ দেখুন এবং ত্রুটি খুঁজুন

Happy coding! 🎉
